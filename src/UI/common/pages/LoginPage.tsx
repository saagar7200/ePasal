import { Box, Input, Text } from "@chakra-ui/react";
import LonginForm from "../molecules/auth/LonginForm";

const LoginPage = () => {
  return (
    <Box>
      <LonginForm />
    </Box>
  );
};

export default LoginPage;
